import React from "react";
import styled from "styled-components";

const HeaderC = styled.div`
  color: var(--primary);
`;

const Header = () => {
  return (
      <HeaderC>Esto es el header</HeaderC>
  );
};

export default Header;
